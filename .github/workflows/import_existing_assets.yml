name: Import Existing Assets - Viral

on:
  workflow_dispatch:

concurrency:
  group: concrete-horizons-viral
  cancel-in-progress: false

jobs:
  import:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set Up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: pip install requests pyyaml

      - name: Import Historical Assets
        run: python import_existing_assets.py --config config/viral.yml
        env:
          FB_PAGE_ID:    ${{ secrets.FB_PAGE_ID }}
          FB_PAGE_TOKEN: ${{ secrets.FB_PAGE_TOKEN }}

      - name: Save to assets.json
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets.json metrics_cache.json || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: import viral historical assets to JSON [skip ci]"
            git pull origin main --rebase
            git push origin HEAD:main
          else
            echo "No new viral assets found."
          fi